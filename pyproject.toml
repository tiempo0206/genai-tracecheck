[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "genai-tracecheck"
version = "1.0.0"
description = "Deterministic quality and privacy checks for OpenTelemetry GenAI traces"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "Tianbo" }]
keywords = ["opentelemetry", "genai", "observability", "security", "llm"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Console",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Software Development :: Quality Assurance",
]
dependencies = ["pydantic>=2.10,<3"]

[project.optional-dependencies]
dev = [
  "build>=1.2,<2",
  "coverage[toml]>=7.6,<8",
  "pytest>=8.3,<9",
  "ruff>=0.11,<1",
]

[project.scripts]
genai-tracecheck = "genai_tracecheck.cli:main"

[project.urls]
Repository = "https://github.com/tiempo0206/genai-tracecheck"
Issues = "https://github.com/tiempo0206/genai-tracecheck/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
genai_tracecheck = ["py.typed"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["genai_tracecheck"]

[tool.coverage.report]
fail_under = 95
show_missing = true
skip_covered = true

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["B", "E", "F", "I", "UP"]
